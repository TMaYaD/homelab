apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/kubernetes-sigs/external-dns.git//kustomize?ref=v0.14.2

patches:
  - target:
      kind: Deployment
      name: external-dns
    path: deployment.patch.yaml
