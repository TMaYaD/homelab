apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/argoproj/argo-cd//manifests/cluster-install?ref=v2.8.2
  #- github-creds.yaml
  - project.yaml

patchesStrategicMerge:
  - argocd-cm.yaml
  - service.yaml
